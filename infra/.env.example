# Infra environment — copy to .env and edit before docker compose up.

# Postgres (shared by Supabase-self-hosted + Langfuse)
POSTGRES_PASSWORD=postgres

# Langfuse
NEXTAUTH_SECRET=change-me-in-prod
SALT=change-me-in-prod
