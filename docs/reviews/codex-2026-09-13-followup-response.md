# Response to the Codex follow-up review of 13 September 2026

Reviewed revision named by the review: `ef99adf4f53292e24bb7b3493af87ccd5d7eb37c` (main after PR #147,
13 September 2026), against baseline `4e337c765c141bfcc909c6da53343c640bd5b5aa`.

Actual HEAD when this response started: `a5bd38d` on `claude/relaxed-babbage-of6lum`, six commits
after `ef99adf` (`9e52be3`, `ece29a1`, `2f13820`, `d707bfb`, `caa0bf1`, `a5bd38d`; the branch was
rebased onto `ef99adf` before this work). Every finding below was re-checked on `a5bd38d`, not on
`ef99adf`, and the table says where a later commit had already narrowed a finding.

Companion artefacts named by the review (`CLARA-follow-up-review-2026-09-13.md`,
`CLARA-followup-evidence.zip`, the probe scripts and their outputs) were not available in the
environment that produced this response. The probes were rebuilt from the finding descriptions
(`apps/api/app/tests/_probes_followup/` during the reproduction phase; deleted once each probe
became a regression test). Nothing below claims to have read the bundle.

Environment limits that shape the evidence: outbound HTTPS from this environment goes through a
policy proxy that refuses `api.clara.odradekai.com` (HTTP 403 from the proxy for `/health`,
`/ready`, `/ready/details`), so live verification of production rests on the GitHub Actions
uptime logs, which are read-only and dated. A scratch PostgreSQL 16 (`clara_test`; rebuilt during this
response from an empty database by `scripts/apply_migrations.py` with `postgresql-16-pgvector` 0.6.0
installed from the distribution package, migrations 001–017) is available for parity tests. No model was called; no participant data, no
production mutation, no connector action.

## 1. Disposition table (written before any edit)

Status vocabulary: **accepted** (reproduced at HEAD, fix in this response), **partially accepted**
(the defect is real but the requested remedy or framing is changed, reason given), **challenged**
(counter-evidence at HEAD; nothing to change), **already addressed** (fixed by a commit after
`ef99adf`, regression test added or cited).

| ID | Status | Evidence at current HEAD (`a5bd38d`) | Implementation / test required | External dependency |
|---|---|---|---|---|
| F1 title race → unreviewed outbound content | **Accepted** | Reproduced on the memory and SQLite stores: a `PATCH /problems/{id}` whose persistence was paused after the route's approval check (`routers/problems.py:389-400`) returned 200 after the approval was recorded; the retry then sent `UNREVIEWED replacement title [structural]` with `human_reviewed=true` (probe `probe_f1_race.py`; both stores). Root causes: (a) check-then-write with no lock (`update_problem`, `update_action_proposal`); (b) `DISPATCH_SNAPSHOT_FIELDS` covers action fields only (`workflow.py:267`), so `authorize_dispatch` cannot see a title or statement change; (c) `_build_push_payload` rebuilds the payload from the *current* problem at dispatch (`action_push.py:105-136`). Already narrowed by `d707bfb`: numeric decision order and a forced `refresh()` before authorisation (cache staleness), which do not touch (a)–(c). | Freeze the outbound content on the approval (`outbound_snapshot` + SHA-256 of title, statement, severity, evidence ids, action text); `authorize_dispatch` refuses `problem_changed_since_approval` on drift; `_dispatch` sends the frozen approved content (admin routing/config still resolved at dispatch and named in the audit detail). Per-problem governance lock around check+write on every store; PostgreSQL problem writes take `pg_advisory_xact_lock` and re-run the approval guard inside the same transaction, approval inserts take the same lock. DB-level dispatch claim (`claim_dispatch`/`release_dispatch`, TTL) on memory, SQLite and PostgreSQL; a claimed execution is refused with a distinct reason. Post-push re-check: a decision recorded while the connector was writing is annotated on the execution and emitted as telemetry (the external record cannot be recalled; documented). Tests: overlapping edit/approval on memory and SQLite (edit refused or retry refused with no connector call), existing retry/revocation/supersession/four-eyes/dependency tests unchanged, PostgreSQL two-instance cache-vs-committed rejection, two-instance claim. | None for the fix. A live multi-worker deployment test is not possible here. |
| F2 pending plans not bound to their contract | **Accepted** | Reproduced: plans scheduled under contract revision 1 (`plans.contract_revision == 1`); after `PATCH` success_threshold 0.0001 → 100 (revision 2) the due read was stamped `contract_revision=2`, `threshold=100` and scored `target_met` (`probe_f2_f3.py`). `run_due_measurements` stamps `problem.outcome_contract` at read time (`measurement_scheduler.py:572-573`); the plpgsql tick does the same (`016:...contract_snapshot, problem_payload->'outcome_contract'`). The plan's `contract_revision` column is written but never read. | Plans freeze the complete scoring terms (`contract_snapshot`) at scheduling; the Python and plpgsql ticks score under the plan's frozen terms; a contract amendment explicitly supersedes the problem's pending plans and re-plans them under the new revision on the same clock (audit event `measurement_replanned` with the term diff); legacy plans without a frozen snapshot are measured only when the problem's current revision equals the plan's recorded revision (deterministic link), otherwise marked `blocked` with the reason. Migration `017` (columns + function replacement; 016 is not edited). Tests for target, baseline, metric, direction (baseline/target flip), method and window amendments on SQLite and PostgreSQL. | Migration 017 must be applied by the owner (SQL editor) before the PostgreSQL tick uses frozen terms; the API self-heals the columns and falls back safely. |
| F3 observation interval moves with a late worker | **Accepted** | Reproduced: window 7 days, 60 in-window signals, 10 later signals, tick run on day 30 → the checkpoint read `2.3333/day from 70 signals` instead of the planned `8.5714/day` (60/7) (`probe_f2_f3.py`). `run_due_measurements` reads `[executed_at, now]` (`measurement_scheduler.py:535-561`); the plpgsql tick uses `_now` for both the `BETWEEN` and `elapsed_days`. The follow-up start is fixed (`due_at − 30 d`) but its end is also `_now`. | Plans carry `observation_start`/`observation_end` fixed at scheduling (t7/window: `[executed, executed+k d]`; follow-up: `[due−30 d, due]`); both ticks read exactly that interval with inclusive bounds; `measured_at` stays the processing time; readings carry the interval; the enrichment-completeness check and guardrails use the same bounds; the read-time cumulative ITS stays labelled as live. Same-timestamp ordering rule: latest reading = greatest `(observation_end, measured_at, checkpoint rank, plan id)`. Tests: exact-due vs late worker give the same reading, delayed enrichment, three overdue kinds in one tick, follow-up boundaries, rerun idempotence, on SQLite and PostgreSQL. | Migration 017 (same as F2). |
| F4 evidence grade by substring; legacy borrowing; causal wording | **Accepted** | Reproduced at HEAD: `uncontrolled_before_after → A`, `matched_control → A` (no realised control data), `controll → A`, `holdout_typo_hodlout → A`, `randomized → D`, `sparse_its → C` (`outcome_engine.evidence_grade`, line 97: `"control" in method`). `loop_verdict` certifies a legacy reading (no `checkpoint_kind`) whenever *any* closing plan of the problem is done, whatever execution produced it (`outcome_engine.py:604-606`). The verdict note reads "the fix did not land" for an uncontrolled read. | Explicit method → grade map (A only for `randomized_holdout` with a realised holdout; B reserved; C for ITS with a realised fit, D when the fit fell back; D for every before/after including `uncontrolled_*`; E for manual/unknown methods), negative tests for `uncontrolled`, `matched_control` without realised data, misspellings and unknown methods. Legacy readings: provenance `unknown` unless the reading's `plan_id`/`execution_id` links deterministically to a done closing plan of the same problem; never certified from another execution's plan; certified readings check problem/execution/plan/contract compatibility. Verdict note: "observed no improvement"; attribution stated only when the design supports it. | None. |
| F5 wholly invalid runs vanish; stale files; unanswered predictor | **Accepted** | Reproduced: two wholly failed predictors → 0 accuracy rows, 0 pair rows, and `_write` left a pre-existing `compare_runs_accuracy.csv` untouched (`compare_runs.py:183-185`); valid floor + one failed predictor → the failed predictor is absent from the table and no pair is reported (`probe_f5_f7.py`). `run_eval.equity_slices` `continue`s on an unanswered predictor (`run_eval.py:316-327`). | Coverage/count rows for every run and task (n_expected, n_valid, coverage, end-to-end accuracy 0.0 when nothing valid, quality metrics `null`); pairs listed with `n_pairs = 0` and `p = null` when no eligible pair; `_write` always writes the header (explicit empty table); equity rows carry `recall_<p> = null`, `recall_<p>_end_to_end = 0.0`, `n_scored_<p> = 0`; CLI tests for (a) valid floor + one failed predictor, (b) two wholly failed predictors. | None. |
| F6 run fingerprint misses the effective inputs | **Accepted** | Reproduced: editing `SYSTEM_PROMPT`, setting `ENRICH_TAG_CANON=0` and reversing the exemplar order changed **no** key of `_run_config` (`probe_f9_f6.py`); `_run_config` hashes golden bytes, held-out ids and an order-free exemplar canonical form only (`run_live.py:334-356`). | `config.inputs` block: effective system prompt hash (as assembled with the fewshot block and injection guard), tool schema hash, ordered-exemplar hash beside the order-free one, model, temperature, batch size, `ENRICH_TAG_CANON`, `ENRICH_ROUTING_CLOSED_SET`, `ENRICH_FEWSHOT`, dataset/split hashes, harness commit and dirty state with a hash of the harness source files; `experimental` (knobs the loop turns) kept separate from `nuisance` (environment) so a report says which changed; rejected candidates carry the same block (the ledger row does). No network in tests; no secrets in the manifest. | None. |
| F7 held-out split used in selection through the safety veto | **Partially accepted** | Reproduced: two reports identical on the optimisation split; only held-out items hallucinate in the new run → `guards.hallucination.status == "rose"` (`probe_f5_f7.py`), so the pooled veto *does* consult the held-out split while `LOOP_PROMPT.md:115` says the split "plays no part in accept/reject". The veto itself is the documented intent (`LOOP_PROMPT.md` targets: "pooled over every scored item"). The review's preferred remedy is adopted: keep the veto, name the split honestly. | Guards report their scope (`pooled`, per-split eligible/flagged counts, `held_out_contributes`); LOOP_PROMPT and the manuscript call the held-out split a *safety-validation set used in selection* for the veto and *not used* for the accuracy decision; the "replication substitutes for alpha control" sentence is removed (LOOP_PROMPT and deck) and the loop is described as exploratory; test where only held-out safety changes. | None. |
| F8 second-rating provenance | **Accepted, as framed by the review** | Repository facts unchanged since the 12 September record: `kappa_sample.csv` (40 rows, all `blind_risk` filled) and `kappa_risk.json` were committed together in `24d56fc`, 29 minutes after the rubric; nothing committed identifies the rater. The review's new observation (a Codex task "Fill blind risk labels", id `01a077a7-b101-78c0-accc-97f813223a23`, all 40 rows identical to the committed file) is reported by the reviewer and **cannot be verified here**; the owner's 12 September statement ("another rater") remains an attestation. With an AI-produced label set identical to the file, the committed rating no longer supports the phrase "the one human check"; what is supported is *second-rating agreement with independent human provenance unverified*. κ values and files are unchanged. | Replace every human-validation claim about the rating (03:71, 03:126, 05:100, 06:49, 02:335, App. D:33, ledger R0.1–R0.4, board spec + canvas, deck source + rebuilt binary); `PROVENANCE_kappa.md` §5 records the review's observation (as reported, unverified) and the distinction between observed evidence and attestation; consistency test forbids "human check" wording about the rating. No new rating is required for this response. | A rating with recorded human provenance (rule in `PROVENANCE_kappa.md` §6) is the only way to restore a human-check claim; owner's decision. |
| F9 survey script issues verdicts from n = 10 | **Accepted** | Reproduced: `analyse()` returns `CONFIRMED` for H1, H2 and H8 at n = 10 and n = 39 (`MIN_N = 10`, `survey_analysis.py:44`), while the pre-registered rule (05:170) issues verdicts only at n ≥ 40 and ledger row R3.2 records the mismatch as "the script was not changed". | Tiered output: `n < 10` → counts only; `10 ≤ n ≤ 39` → descriptive (proportion + Wilson interval, verdict `DESCRIPTIVE ONLY (n < 40)`); `n ≥ 40` → verdict, where n is each hypothesis's usable denominator and H2/H8 also need their corroborating item at n ≥ 40; fragile flag kept. Tests at n = 9, 10, 39, 40, missing answers, fragile intervals, failed corroboration. Manuscript 05:170 and ledger R3.2 updated. | None. |
| Implementation-recording UI | **Accepted** | `POST /problems/{id}/executions/{id}/implementation` exists with validation (future, before approval, before dispatch, authorisation) but the web app has no client function and no control; `ExecutionRecord` in `lib/types.ts` lacks `dispatched_at`/`implemented_at`. Ticket creation never sets `implemented_at` (dispatch stamps `dispatched_at` only). | Editor-gated form on the action decision panel (instant + note, client-side validation, server errors surfaced), execution row shows dispatch/implementation instants and the checkpoint origin; superseded plans stay visible in the checkpoints panel; API tests for authorised, viewer-denied (403), invalid/future/before-dispatch instants; `web:lint` + `web:build`. | None. |
| CI wiring (thesis checks, adversarial regressions, PostgreSQL) | **Accepted** | `npm run check` = api lint + api tests + web lint + build; `thesis:test` and `thesis/test_manuscript_consistency.py` are not part of it; CI has no PostgreSQL service, so `test_pg_parity_measurement.py` is skipped on every run; no migration-upgrade test. | `check` includes `thesis:test` and the manuscript consistency test; CI job with a disposable PostgreSQL 16 service applying 001–017 in the documented order, running the parity/cache/concurrency/backlog-ordering tests, plus an upgrade test (pre-017 schema + old function → 017 → safe rerun). | None. |
| Release / `/ready` | **Accepted (diagnosis confirmed; still failing)** | Uptime run 869 (13 Sep 2026 06:32 UTC, twice 60 s apart): `/health → 200`, model-card `401`, `/ready → 404 {"detail": "Not Found"}`; every other check passes. The 404 body is FastAPI's default, so the running image serves a build without the `/ready` route (added 2 Sep, `01ced20`), which is consistent with version lag but does not identify the deployed commit: the API exposes no version endpoint. Direct probing from here is blocked (proxy 403). | `/health` gains `version` (git commit baked into the image, `CLARA_BUILD_COMMIT`) and `/ready` reports schema/function compatibility (measurement function signature present and at the expected version, plan columns present) with detail behind the existing auth; tests for old-function/new-app, DB failure, compatible. DEPLOY.md: migration → deploy → smoke → rollback as one reviewable release step; the smoke script prints the served version. Nothing is deployed by this response. | Owner: rebuild and restart the API container, apply 017, run the smoke. |
| "Stale response text saying taxonomy scoring is unvalidated" | **Challenged** | No such text at HEAD: `grep -i "unvalidated\|not validated"` over `docs/reviews/codex-2026-09-12-response.md`, `thesis/CLAIM_LEDGER.md` and the manuscript returns only the E1 row's description of the *old* scorer and an unrelated §4 sentence about learnings; the response's §1.2 states that `score_taxonomy` goes through the validator. Nothing to remove; if the reviewer's bundle names a specific sentence, it is not in the repository. | None. | None. |
| August-vs-September "pipeline only" contrast | **Accepted** | 05:87 states the GLM-5.2 generic run (4 August) and the production-stage run (5 September) differ *only* in the pipeline; 05:98 calls the same pair "the *pipeline* effect", while 05:102 itself records that the September generic repeats disagree with the August run on 14 % of risk labels. | Reword as a configuration contrast (prompt/pipeline × run date, one model behind a gateway that may have changed) in 05:87, the table label and 05:98/05:102; ledger row. | None. |

## 2. Reproduction log (before edits, HEAD `a5bd38d`)

| Finding | Probe | Output |
|---|---|---|
| F1 | `_probes_followup/probe_f1_race.py` (memory, SQLite) | `PATCH status after approval: 200`; `retry status: 200 jira calls: 2`; `title sent on retry: UNREVIEWED replacement title [structural] human_reviewed: True` — both stores |
| F2 | `probe_f2_f3.py` | `plan revisions at scheduling: {'t7': 1, 'followup': 1}`; `contract revision after PATCH: 2`; `reading scored under revision 2 threshold 100.0 -> status target_met` |
| F3 | `probe_f2_f3.py` | `latest reading kind/value: t7 2.3333`; `planned window rate 60/7 = 8.5714; cumulative-to-now rate 70/30 = 2.3333`; plan note `observed 2.3333/day from 70 signals` |
| F4 | inline | `uncontrolled_before_after → A`, `matched_control → A`, `randomised_holdout → A`, `randomized → D`, `sparse_its → C`, `controll → A`, `holdout_typo_hodlout → A`, `unknown_method → D` |
| F5 | `probe_f5_f7.py` | `two wholly failed predictors -> accuracy rows: 0 pair rows: 0`; stale file after `_write`: `task,run,n | sentiment,OLD_RUN,999`; `valid floor + failed predictor -> runs in accuracy table: ['floor'] pairs: []` |
| F6 | `probe_f9_f6.py` | `config keys that change after prompt edit + canon off + exemplar reorder: NONE` |
| F7 | `probe_f5_f7.py` | `guard status when only held-out items hallucinate: rose` |
| F9 | `probe_f9_f6.py` | `n=9 -> INSUFFICIENT N`; `n=10 -> H1 CONFIRMED / H2 CONFIRMED / H8 CONFIRMED`; `n=39 -> CONFIRMED`; `n=40 -> CONFIRMED` |
| `/ready` | GitHub Actions uptime run 869 (job 103685774369) | `[FAIL] readiness probe (/ready -> 200 ...) — got 404 {'detail': 'Not Found'}`, `21/22 checks OK` on both attempts |

The probe directory is not part of the delivered tree: each probe became a regression test of the
desired invariant (section 4 names them).

## 3. Work packages, commits and acceptance results

Commits on `claude/relaxed-babbage-of6lum` after `a5bd38d`, in order; each is reviewable on its own.

| Commit | Package | What landed | Acceptance evidence |
|---|---|---|---|
| `d29b8b6` | WP1 (F1) | `OutboundContent` frozen on the approval (`outbound_snapshot`, `outbound_sha256`, preview route `GET /problems/{id}/actions/{id}/outbound-preview`); `authorize_dispatch` refuses `problem_changed_since_approval`, `approval_unverifiable` (a pre-binding approval attests but cannot dispatch) and `dispatch_in_progress`; `expected_outbound_sha256` on decisions (stale hash → 409, nothing recorded); per-problem `problem_lock` on memory/SQLite and `pg_advisory_xact_lock` on PostgreSQL around problem/action edits and approval inserts, with the guard re-run inside the transaction; `claim_dispatch`/`release_dispatch` compare-and-set with a 300 s TTL on all three stores; the push sends the frozen content; a decision recorded while the connector was writing is annotated on the execution and emitted as `action_pushed_during_revocation`. | `test_dispatch_binding.py` (10 tests: title changed behind the API refuses the retry on memory and SQLite; edit and approval serialised; frozen content is what dispatch sends; stale expected hash refuses the decision; four-eyes reviewers must sign the same content; pre-binding approval covers the attestation only; snapshots persist; a live claim refuses the retry and an expired one is taken over; the claim is a compare-and-set; rejection during the connector call annotated and reported). `test_pg_parity_governance.py` (5 tests on PostgreSQL, two store instances: committed rejection beats a cached snapshot; DB compare-and-set claim; edit committed while a decision waited → 409; decision first → edit guard refuses; JSONB round trip). |
| `bac90c3` | — | Sections 1–2 of this response (dispositions before any edit; reproduction log). | — |
| `be86add` | WP2 (F2, F3, F4, UI) | Plans freeze `contract_snapshot` and `observation_start`/`observation_end` at scheduling; both ticks score the frozen terms over the fixed interval (`plan_scoring_terms`, `plan_observation_interval`; legacy plans without frozen terms measured only under an unchanged revision, else `blocked`); `replan_after_amendment` supersedes and reschedules pending checkpoints on the same clock (`measurement_replanned`); latest-reading rule `outcome_order_key`; explicit `DESIGN_GRADES` and realised-design `evidence_grade`; `_certifying_plan` binding; verdict "no improvement observed" with attribution stated separately; live ITS estimate graded on its fit and labelled read-time; migration `017_measurement_intervals.sql` (016 untouched); web: implementation-recording form, preview hash on decisions, interval and revision on the checkpoints panel. | `test_measurement_intervals.py` (11 tests: late worker = punctual worker on memory and SQLite; interval and terms on the plan; three overdue kinds in one tick; latest by interval not processing order; late T+7 blocked; theme enrichment inside the interval only; guardrails bounded; amendment re-plans; guardrail-only amendments do not; frozen terms behind the API; legacy plan blocked then measured when the link holds), `test_evidence_grades.py` (7), `test_implementation_record_api.py` (3 tests, 5 cases: viewer 403 / editor records; malformed, future and pre-approval instants 422 and record nothing; pre-dispatch refused and a created ticket never counts), updated `test_measurement_semantics.py`, `test_dispatch_authorization_gaps.py`, `test_loop_closure.py`, `test_review_phase_c2.py`, `test_works_council_walker.py`; `test_pg_parity_measurement.py` (8 on PostgreSQL, payload keys incl. the interval). |
| `3e86f66` | WP3 (F5, F6, F7, F9) | `compare_runs.py`: fixed column sets, a row for every run and task (`n_expected`, `n_valid`, `n_invalid`, `coverage`, end-to-end accuracy 0.0, quality metrics empty) and for every absent pair (`n_pairs = 0`), `_write` always writes the header; `run_eval.py`: unanswered-predictor equity rows; `run_live.py`: `config.inputs` (experimental: model, temperature, batch size, effective system-prompt hash via `build_system_prompt`, tool-schema hash, ordered and order-free exemplar hashes, flags; nuisance: dataset and split hashes, harness commit, dirty state, harness source hash, Python version; rejected candidates carry it in the ledger row), `inputs_changed`, guards with `scope`, per-split eligible/flagged counts and `held_out_contributes`; `LOOP_PROMPT.md`: the held-out split is a safety-validation set used in selection, replication is not alpha control, the loop is exploratory; `survey_analysis.py`: counts only below n = 10, `DESCRIPTIVE ONLY (n < 40)`, verdicts from n = 40 per usable denominator with corroboration at n ≥ 40; `npm run check` now runs `thesis:test` and the manuscript consistency checks. | `thesis/evaluation/test_compare_runs_cli.py` (cases a and b through the real CLI, stale files never survive), `thesis/evaluation/test_survey_analysis.py` (n = 9/10/39/40, per-hypothesis denominators, corroboration at n < 40 and failed, fragile only at the verdict tier), `test_run_live_reporting.py::TestInputFingerprint` and `::test_guard_scope_names_the_held_out_split_as_a_safety_validation_set`, `test_eval_harness.py`. |
| `140a6b5` | WP5 (CI, release, `/ready`) | `GET /health` carries the build identity; `GET /ready` adds `measurement_schema` (never a 503 on its own); `/ready/details` names expected vs found function version, missing columns, tick and action; `measurement_schema_state`, `measurement_compatibility`; Dockerfile `GIT_COMMIT` build arg; `scripts/apply_migrations.py` (documented order, API boot DDL between 008 and 009, one transaction per file, pgvector check); `test_pg_migration_upgrade.py`; CI job `postgres-parity` on `pgvector/pgvector:pg16`; smoke prints the served build and the schema state; DEPLOY.md release step, 014/016/017 in the migration list, observed `/ready` state. | `test_readiness_diagnostics.py` (16 cases: compatibility matrix, build identity, public `/ready` shape, old function under a new app reported not evicted, Python fallback, database failure → 503 with the error class only, catalogue reads), `test_pg_migration_upgrade.py` (2 on PostgreSQL: the four-step upgrade; the script's plan and its refusal of unlisted files), fresh-database chain on the scratch server (§4). |
| `aab9aaa` | WP4 (F8, F9, F7 wording, F1–F4 sentences, Aug/Sep contrast) | Manuscript, ledger, deck source, board spec, provenance record and notes reworded; `defense_deck.pptx`, `board/canvas.html` and `build/thesis.docx` regenerated; six new consistency checks. | `test_manuscript_consistency.py` 15/15 after the re-point below; deck fit check; rendered-text inspection (§4). |
| this commit | — | Claim ledger re-pointed (53 citation groups) at the commits that last touched each artefact; sections 3–6 of this response. | `test_manuscript_consistency.py` 15/15. |

Probe → regression test (the probes of §2 were deleted once each became a test): F1 →
`test_dispatch_binding.py::test_title_changed_behind_the_api_after_approval_refuses_the_retry`
and `::test_edit_and_approval_of_one_problem_are_serialised`; F2 →
`test_measurement_intervals.py::test_reading_is_scored_under_the_plans_frozen_terms_when_the_contract_changed_behind_the_api`;
F3 → `::test_late_worker_reads_the_same_interval_as_a_punctual_one`; F4 →
`test_evidence_grades.py::test_instrumented_grades_follow_the_explicit_design_table`,
`::test_no_grade_a_or_b_is_reachable_today`,
`::test_legacy_readings_have_unknown_provenance_and_manual_readings_never_certify`; F5 →
`thesis/evaluation/test_compare_runs_cli.py`; F6 → `test_run_live_reporting.py::TestInputFingerprint`;
F7 → `::test_guard_scope_names_the_held_out_split_as_a_safety_validation_set`; F9 →
`thesis/evaluation/test_survey_analysis.py::test_tiers_at_9_10_39_and_40`; `/ready` →
`test_readiness_diagnostics.py` and `test_pg_migration_upgrade.py`.

## 4. Validation record

Exact invocations and the outputs they gave, at the state named. Nothing here ran a model, read or
wrote participant data, wrote to production or called a connector.

| Check | State | Command | Result |
|---|---|---|---|
| API lint | `140a6b5` and final HEAD | `cd apps/api && python3 -m ruff check --select F app scripts ../../scripts` | `All checks passed!` |
| API tests | `140a6b5` (no API code changed after it) | `npm run api:test` | `1085 passed, 15 skipped, 46 warnings in 216.87s`; the 15 skipped are the three PostgreSQL files without `CLARA_TEST_DATABASE_URL` |
| Migration chain, fresh database | `140a6b5` | `psql … -c "DROP DATABASE clara_test WITH (FORCE)" -c "CREATE DATABASE clara_test"; python3 scripts/apply_migrations.py postgresql://clara@127.0.0.1:54329/clara_test` | plan `001, 002, 004, 003, 005, 006, 007, 008, API boot DDL, 009, 010, 011, 013, 014, 015, 016, 017, 012`; 011 warns that pg_cron is unavailable (tick stays in-process); final line `measurement schema: function_version=17 tick=plpgsql columns_missing=[]`. A first attempt with the API DDL before 001 failed at 004 (`function public.is_current_workspace(bigint) does not exist`), which is why the DDL sits between 008 and 009 |
| PostgreSQL parity and governance | `140a6b5`, scratch server | `CLARA_TEST_DATABASE_URL=… python3 -m pytest -q app/tests/test_pg_parity_measurement.py app/tests/test_pg_parity_governance.py` | `13 passed in 2.30s` |
| PostgreSQL upgrade | `140a6b5`, scratch server | `… python3 -m pytest -q app/tests/test_pg_migration_upgrade.py` | `2 passed in 1.41s` (the test creates and drops `clara_upgrade_<hex>` on the server) |
| Thesis checks | final HEAD | `npm run thesis:test` | every script passes; `test_manuscript_consistency.py`: 15 ok (before the re-point, `test_ledger_commits_are_the_last_to_touch_each_artefact` listed the citations WP1–WP5 had made stale) |
| Web lint | `aab9aaa` (web unchanged since `be86add`) | `npm run web:lint` | `0 errors, 2 warnings` — both pre-existing `@next/next/no-img-element` warnings in `apps/web/app/(public)/landing.tsx`, a file no commit here touches |
| Web build | `aab9aaa` | `npm run web:build` | exit 0, 39 static pages |
| Deck | `aab9aaa` | `node thesis/defense/make_deck.js && node thesis/defense/check_fit.js` | `written`; `5 box(es) estimated to overflow` on slides 24 (1.64×, 1.04×), 6, 8 and 2 — the five that predate the reviews; the first WP4 wording added a sixth (slide 16, 1.02×) and the bullet was shortened until the check returned to five |
| Board | `aab9aaa` | `python3 thesis/board/build_canvas.py` | `wrote ../board/canvas.html`; the new wording occurs once in the rendered canvas |
| Manuscript | `aab9aaa` | `PANDOC=… bash thesis/build_docx.sh`; `pandoc thesis/build/thesis.docx -t plain` | `built: 2.0M thesis.docx`. Rendered text: "independent human provenance unverified" ×3 and "… is unverified" ×4, "Fill blind risk labels" ×1, "no improvement observed" ×1, "prompt/pipeline × run date" ×1, "DESCRIPTIVE ONLY (n < 40)" ×1, "safety-validation set" ×2, "explicit table of methods" ×1; 0 for "the one human check", "only human check", "blind human rating", "on the owner's attestation", "blind second rating", "blind double-labelling", "the pipeline barely moves", "never optimised against", "writes a support label from n = 10"; "fix did not land" ×1, in the new §4 sentence that says the platform does not say so |
| Deck XML | `aab9aaa` | `unzip defense_deck.pptx; grep` over `ppt/slides/*.xml` and `ppt/notesSlides/*.xml` | "Second-rating agreement" ×1, "provenance unverified" ×3, "do not replace alpha control" ×1; 0 for "Inter-annotator", "on attestation", "blind second", "three consecutive significant runs", "AI exposure unrecorded"; 26 slides |
| Live production | — | `scripts/live_smoke.py` cannot reach `api.clara.odradekai.com` from here (proxy 403) | The state stands as of uptime run 869 (§2): `/ready → 404`, everything else green. Not re-verified |

## 5. Source-to-claim ledger

| Claim made in this response | Source | Evidence type |
|---|---|---|
| Production answers `/ready` with a FastAPI-shaped 404 and `/health` with 200 (13 Sep 2026 06:32 UTC) | GitHub Actions uptime run 869, job 103685774369 | CI log, observed, dated |
| The deployed commit cannot be identified | the same log: the deployed `/health` body carries no version | inference from the log; resolved by the next build that carries `GIT_COMMIT` |
| The full migration chain applies on an empty PostgreSQL 16 with pgvector and `clara_measurement_function_version()` returns 17 | `scripts/apply_migrations.py` output on the scratch server (§4) | observed on a disposable database; not production |
| The API boot DDL must run between 008 and 009 | the failed first attempt (`is_current_workspace(bigint)`) and the successful run (§4) | observed |
| `1085 passed, 15 skipped`; `13 passed`; `2 passed`; consistency `15 ok` | pytest and script output (§4) | observed |
| F8: the filled sample and its κ file were committed together 29 minutes after the rubric, under the author's git identity; nothing committed identifies a rater | `git log` (`c2d6f57`, `24d56fc`, `5ee0c35`), `PROVENANCE_kappa.md` §2 | repository evidence |
| F8: an AI-assistant task record "Fill blind risk labels" (`01a077a7-b101-78c0-accc-97f813223a23`) holds 40 labels identical to the file | the follow-up review of 13 Sep 2026 | reviewer-reported; not committed; not verifiable here |
| F8: a second person produced the labels | the owner's statement of 12 Sep 2026 ("another rater") | attestation |
| F8: κ = 0.55 unweighted (0.38–0.72), 0.70 linear-weighted (0.56–0.82), 27/40, 38/40 | `thesis/evaluation/results/kappa_risk.json`, `kappa_sample.py score` | reproducible from committed files; unchanged |
| The September generic repeats agree with the August generic run on 85.8–86.8% of risk labels | `thesis/evaluation/results/compare_runs_agreement.csv`, rows `glm_generic_run0_vs_glm_generic_run{1,2,3}` | committed results file; no rerun |
| `survey_analysis.py` issued `CONFIRMED` from n = 10 before `3e86f66` | probe output (§2) against `MIN_N = 10` | observed |
| Five deck overflows predate the reviews; none new | `check_fit.js` output before and after the bullet change | observed (the check is a heuristic estimate, not a renderer) |
| The two web lint warnings are pre-existing | eslint output naming `landing.tsx:153` and `:236`; `git log` shows no commit here touched the file | observed |
| Nothing in this response reports a human validation, a new model-evaluation result or a participant finding | — | by construction: no model call, no participant data, no production write |

## 6. Remaining external dependencies

1. **Production release (owner).** Apply `017_measurement_intervals.sql` in the Supabase SQL editor,
   rebuild the image with `--build-arg GIT_COMMIT=$(git rev-parse --short HEAD)`, run
   `scripts/live_smoke.py`, read `/ready/details` (DEPLOY.md release step; written, not performed).
   Until then: `/ready` keeps answering 404; the pg_cron tick on production runs the 016 function,
   so any checkpoint that falls due there is read open-ended under the current contract terms, and
   the plans the new API writes (frozen terms, fixed intervals) are ignored by that tick. The API
   self-heals the plan columns on boot either way, and `GET /ready` will say `incompatible` until
   017 is applied.
2. **A rejection that arrives after the external call began (F1).** The dispatch claim is taken
   at the store before authorisation, the authorisation is re-checked under the claim, and the
   connector call itself cannot be interrupted. If a rejection commits after the call started, the
   external record exists: the execution is annotated `WARNING: <reason> recorded during dispatch …
   needs manual withdrawal`, `action_pushed_during_revocation` telemetry is emitted, and the
   execution is never retried. No automatic withdrawal is attempted, because a connector-side
   delete would be a second unreviewed external action. Test:
   `test_dispatch_binding.py::test_rejection_recorded_during_the_connector_call_is_annotated_and_reported`.
3. **CI.** `.github/workflows/ci.yml` runs on pull requests and on pushes to `main`; a push to this
   branch alone does not run it, so the first `postgres-parity` run is pending the pull request.
   Its service image `pgvector/pgvector:pg16` is public; a pull failure fails the job at service
   start, visibly.
4. **F8 provenance (owner).** Either the rater's role, hand-over and return dates and AI-access
   statement (`PROVENANCE_kappa.md` §6), or confirmation that the task record produced the file
   (then the cross-model wording of `review-2026-09-12-response-notes.md`). Until then every
   document describes the rating as second-rating agreement with independent human provenance
   unverified.
5. **Practitioner study.** Survey and interview data remain to be collected; the reporting tiers
   are now enforced by the script as well as pre-registered.
6. **Live concurrency.** The advisory lock and the dispatch claim are exercised by two store
   instances in one process (threads for the lock); a multi-worker deployment test is not possible
   here.
7. **Environment.** pgvector on the scratch server was installed from the distribution package
   during this response; the environment is ephemeral, and CI carries the reproducible version.

## 7. Second pass, same day: the manuscript against the code, and as research

After the response above the owner asked whether the manuscript reflects the code changes and for a review of the thesis as research. Dispositions and evidence for that pass are in `thesis/CLAIM_LEDGER.md` section H; in short:

- **Stale passages.** Nine passages that the code changes had left behind or that contradicted each other were corrected (ledger H3); Appendix E was re-extracted for migrations 016/017.
- **Multiple comparisons.** The paired McNemar tests were reported unadjusted. `thesis/evaluation/multiple_comparisons.py` applies the Holm step-down within two pre-defined families per task over the committed results (no rerun): the headline sentiment lead reads p = 0.087 adjusted (0.029 unadjusted) and is now reported as a replicated, exploratory lead in §5A.3, §5A.6, §6.4, §7 and the deck (H1).
- **Length and register.** Dated provenance moved out of the chapters into Appendix G (2,117 words); repeated interpretation passages in Chapters 4–7 were compressed; the abstract was rewritten (293 words); a capability table replaced the prose-only competitor comparison. Main chapters: 45,330 → 43,804 words. The remaining length is content whose cutting is the author's call (H2).
- **Tooling for the studies that need humans or paid runs** (H5), each tested on synthetic data, none run on the corpus here: `annotation_kit.py` (two-rater blind labelling, agreement, adjudication, re-score of the committed predictions against a human gold standard), `retrospective_its.py` (the shipped estimator over real dated inflow around a known event), `run_matrix.py` (the matched 2×2 rerun), `predict_embedding.py` (an embedding-classifier baseline). `package.json` `thesis:test` runs their tests.
- **Not done, because it cannot be done from here:** the practitioner interviews and survey, the human annotation study, the matched model rerun, the embedding baseline on the corpus, a self-hosted model run, and the retrospective ITS on real inflow. The remaining-actions list in the ledger names each with its command.
