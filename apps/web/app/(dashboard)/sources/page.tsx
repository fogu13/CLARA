// Sources — port of reference/elvis/src/pages/Sources.tsx
// TODO(Phase 4): signal source cards with status, webhook instructions, Refresh,
// add-source dialog (api_poll field-mapping form + Zendesk connector config).

export default function SourcesPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Sources</h1>
      <p className="text-muted-foreground">TODO: port from reference/elvis/src/pages/Sources.tsx</p>
    </div>
  );
}
