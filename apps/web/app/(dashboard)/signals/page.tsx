// Signals — port of reference/elvis/src/pages/Signals.tsx
// TODO(Phase 4): filter sidebar, search, table, CSV import, Analyze button
// (runs enrich + synthesize), signal detail slide-over.

export default function SignalsPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Signals</h1>
      <p className="text-muted-foreground">TODO: port from reference/elvis/src/pages/Signals.tsx</p>
    </div>
  );
}
