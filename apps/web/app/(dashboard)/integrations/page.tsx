// Integrations marketplace — port of reference/elvis/src/pages/Integrations.tsx
// TODO(Phase 4): connector catalog by category, active connections, connect dialog,
// API Keys tab. Unlike Elvis, this version actually consumes configs: Jira/Slack
// pushes read from here (Phase 2 connectors).

export default function IntegrationsPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Integrations</h1>
      <p className="text-muted-foreground">TODO: port from reference/elvis/src/pages/Integrations.tsx</p>
    </div>
  );
}
