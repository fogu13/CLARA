// Rules — port of reference/elvis/src/pages/Rules.tsx + RuleBuilderDialog.tsx
// TODO(Phase 4): rules table with enable toggle + edit; builder dialog with chip-style
// condition pickers, action builder, priority, measure-after-days, auto-execute switch,
// inline Relevant Learnings panel.

export default function RulesPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Rules</h1>
      <p className="text-muted-foreground">TODO: port from reference/elvis/src/pages/Rules.tsx</p>
    </div>
  );
}
