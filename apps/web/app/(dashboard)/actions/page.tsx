// Actions / approvals — port of reference/elvis/src/pages/Actions.tsx
// TODO(Phase 4): pending approvals (approve/reject) + history table.
// Approve triggers the real connector push (Jira/Slack) via the LangGraph action node.

export default function ActionsPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Actions</h1>
      <p className="text-muted-foreground">TODO: port from reference/elvis/src/pages/Actions.tsx</p>
    </div>
  );
}
