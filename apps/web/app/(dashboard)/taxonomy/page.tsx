// Taxonomy — port of reference/elvis/src/pages/Taxonomy.tsx
// TODO(Phase 4): tree view, upload CSV/JSON, Discover themes (evolve-taxonomy),
// Run governance (apply_taxonomy_governance), Map signals (backfill-mappings),
// review queue for candidates.

export default function TaxonomyPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Taxonomy</h1>
      <p className="text-muted-foreground">TODO: port from reference/elvis/src/pages/Taxonomy.tsx</p>
    </div>
  );
}
